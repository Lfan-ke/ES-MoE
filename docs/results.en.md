# Results

Every row is one run record in [`results/`](https://github.com/Lfan-ke/ES-MoE/tree/main/results), and
this page is generated from them by `scripts/report.py`. Arms are grouped by backbone, block
configuration and budget, so two different budgets never land in the same average. Read the paired
tables rather than the two means; the protocol and interactive figures are on [Experiments](experiments.md);
cell-by-cell verdicts are on [Judgment lines](JUDGMENT.md).

Every protocol-matrix `best.pt` and its full training arguments live on the
[`checkpoints` branch](https://github.com/Lfan-ke/ES-MoE/tree/checkpoints) (Git LFS, isolated from
`main`; that branch's README states how many): every bucket and routing number recomputes from them.

<figure class="es-fig" id="fig-1">
<figcaption><b>Fig. 1</b><span>Paired deltas across seven generations</span><em>per-seed values and means of the cells below</em></figcaption>
<div class="esmoe-figure" data-figure="generations" data-metrics="both"></div>
<small>Data: <code>results/summary.md</code></small>
</figure>

--8<-- "results/summary.md"

## Same configuration

One model and one protocol, trained on YOLO-Master's fork and on official ultralytics plus this
package, four arms with a seed to a card: A is the fork running its own `yolo-master-n` (four
`ES_MOE` blocks), A0 the same fork without them, B official 8.4.101 with this package's four
`ESMoE` blocks and `recipe="upstream"`, C official without them. A seed's four arms share a card,
so every difference is paired inside one.

The metrics are re-measured by `scripts/measure.py`: each run's model rebuilt from the config it
trained, loaded with the EMA weights in its `last.pt`, and evaluated with one set of validation
settings, so neither framework's own validation enters the comparison; what each trainer measured
stays in its record as `metrics_by_trainer`. Verdicts follow rounds seven (mixed precision) and eight (FP32) of the
[judgment lines](JUDGMENT.md).

<figure class="es-fig" id="fig-2">
<figcaption><b>Fig. 2</b><span>Four differences with 95% intervals</span><em>large dot for the mean, small dots for seeds, grey band for the noise floor</em></figcaption>
<div class="esmoe-figure" data-figure="same-config-differences" data-metrics="both"></div>
<small>Data: <code>results/same_config.md</code></small>
</figure>

--8<-- "results/same_config.md"

## Area buckets

<div class="es-duo">
<figure class="es-fig" id="fig-3">
<figcaption><b>Fig. 3</b><span>Seven generations</span><em>paired deltas by area bucket</em></figcaption>
<div class="esmoe-figure" data-figure="buckets-generations" data-arms="both"></div>
</figure>
<figure class="es-fig" id="fig-4">
<figcaption><b>Fig. 4</b><span>Same configuration</span><em>A − A0 and B − C</em></figcaption>
<div class="esmoe-figure" data-figure="buckets-same-config"></div>
</figure>
</div>

--8<-- "results/buckets.md"

## Router behaviour

<div class="es-duo">
<figure class="es-fig" id="fig-5">
<figcaption><b>Fig. 5</b><span>Concentration against paired delta</span><em>large dots for four-block layouts</em></figcaption>
<div class="esmoe-figure" data-figure="routing-scatter"></div>
</figure>
<figure class="es-fig" id="fig-6">
<figcaption><b>Fig. 6</b><span>Dead experts by balance term</span><em>counted per checkpoint</em></figcaption>
<div class="esmoe-figure" data-figure="dead-experts"></div>
</figure>
</div>

--8<-- "results/routing.md"

## Balancing pressure

The gradient each objective puts on the router's logits at one weight, measured at the mean routing probabilities each checkpoint converged to. Generated by `scripts/pressure.py`.

<figure class="es-fig" id="fig-7">
<figcaption><b>Fig. 7</b><span>Gradient of the balance terms</span><em>at one weight, median over checkpoints</em></figcaption>
<div class="esmoe-figure" data-figure="pressure"></div>
<small>Data: <code>results/pressure.md</code></small>
</figure>

--8<-- "results/pressure.md"

## Release audit

Each row below is derived from the records, the git history and the published artifacts by `scripts/closure.py`, and can be re-checked by running it.

--8<-- "results/closure.md"
